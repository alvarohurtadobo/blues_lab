/// Constantes globales de la aplicación y rutas de recursos empaquetados.
class AppConstants {
  AppConstants._();

  static const String appName = "Blue's Lab";
  static const String appTagline = "For Pokémon Masters";

  /// JSON de demostración del sync grid (subset con nodos de prueba sync/dmax).
  static const String pairGridsDemoAssetPath = 'assets/data/demo/pairgrids_demo.json';
}
